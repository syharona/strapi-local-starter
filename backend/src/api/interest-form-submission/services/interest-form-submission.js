'use strict';

/**
 * interest-form-submission service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::interest-form-submission.interest-form-submission');
